# Groq API keys (free at https://console.groq.com). At least one required for the AI report.
# Extra keys are used as automatic fallbacks if the first is rate-limited.
GROQ_KEY_1=
GROQ_KEY_2=
GROQ_KEY_3=
